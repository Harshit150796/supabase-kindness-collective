Remove these two lines from `src/components/landing/hero/HeroHeadline.tsx`:

- The `<h1>` "Every donation grows into groceries." (lines 32-34)
- The `<p>` "Watch your gift fall, take root, and feed a verified family." (lines 35-37)

Keep the rotating "CouponDonation is Transparent/Trackable/Real-time" label and the "Donate now" / "How it works" buttons intact. No other files change.