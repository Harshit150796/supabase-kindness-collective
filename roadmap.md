# Roadmap

## Done
- [x] Rewrite `WhatWeDo` as a closed-loop donation trace (dark green-tinted inset panel, scroll-scrubbed on desktop, auto-play on touch, resolved state for reduced motion)
  - [x] Stage-3 lock moment: orb → card, lock springs in with slight overshoot, bloom, one ring pulse, downstream path turns emerald
  - [x] Return leg lands with weight: receipt rides home, origin lights up, receipt panel resolves (tethered to the origin on desktop), trace status flips to COMPLETE
  - [x] Every surface/border/shadow/muted text carries the green temperature (no flat grey, no pure black)
  - [x] Springs throughout, no linear tweens on visible motion
  - [x] Verified: build OK, desktop + mobile screenshots, reduced-motion state, keyboard focus on both doors, one h2 / ordered list / hidden decorative SVG, no emojis, no numeric claims

## Ready
- [ ] Re-check headline scale against the hero on the widest breakpoints (currently 97px at 1280 — confirm it stays the largest type on the site at 1920)
- [ ] Consider a short "sample data" footnote under the trace on mobile if users mistake the trace ID for a real order
