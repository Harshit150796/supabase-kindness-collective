# Roadmap

## In progress
- [ ] Rewrite `WhatWeDo` as a closed-loop donation trace (dark green-tinted inset panel, scroll-scrubbed on desktop, auto-play on touch, resolved state for reduced motion)
  - [ ] Stage-3 lock moment: orb → card, lock springs in with slight overshoot, bloom, one ring pulse, downstream path turns emerald
  - [ ] Return leg lands with weight: receipt rides home, origin lights up, receipt panel resolves, trace status flips to COMPLETE
  - [ ] Every surface/border/shadow/muted text carries the green temperature (no flat grey, no pure black)
  - [ ] Springs throughout, no linear tweens on visible motion
  - [ ] Verify build, desktop + mobile screenshots, reduced-motion state, keyboard focus on both doors

## Ready
- [ ] Re-check headline scale against the hero once the section ships (must be the largest type on the site)
